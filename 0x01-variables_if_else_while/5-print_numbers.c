#include <stdio.h>
/**
* main - prints all numbers of base 10 from 0 to 9,
* followed by a new line,
* Return: Aways 0 (Sucess)
*/
int main(void)
{
int i = 0;
for(; i < 10; i++)
{
printf("%d", i);
}
printf("\n");
return (0);
}
